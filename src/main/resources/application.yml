spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/evolucao_fisica
    username: evolucao_fisica
    password: evolucao_fisica

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080